<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Listings</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style.css" />
  </head>

  <body>
    <header class="fixed-top">
      <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
          <!-- Logo + Brand -->
          <a class="navbar-brand d-flex align-items-center" href="/listings">
            <img
              src="/images/logo.png"
              alt="Logo"
              width="45"
              height="45"
              class="me-2 rounded-circle border border-2 border-primary"
            />
            <span class="fw-bold text-primary fs-4">Resorts</span>
          </a>

          <!-- Toggle button for mobile -->
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarContent"
            aria-controls="navbarContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>

          <!-- Links + Search -->
          <div class="collapse navbar-collapse" id="navbarContent">
            <!-- Navigation links -->
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a
                  class="nav-link active fw-semibold"
                  aria-current="page"
                  href="/listings"
                  >Home</a
                >
              </li>
              <li class="nav-item">
                <a class="nav-link fw-semibold" href="#">About</a>
              </li>
              <li class="nav-item">
                <a class="nav-link fw-semibold" href="#">Contact</a>
              </li>
            </ul>

            <!-- Search bar -->
            <form class="d-flex ms-lg-3 mt-2 mt-lg-0">
              <input
                class="form-control me-2 rounded-pill"
                type="search"
                placeholder="Search resorts..."
                aria-label="Search"
              />
              <button class="btn btn-primary rounded-pill px-3" type="submit">
                Search
              </button>
            </form>
          </div>
        </div>
      </nav>
    </header>

    <div class="container">
      <div class="mx-auto" style="width: 100%; max-width: 33rem; border: none">
        <% if(success && success.length){ %>
        <div class="container mt-3">
          <div
            class="alert alert-success alert-dismissible fade show"
            role="alert"
          >
            <%= success[0] %>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="alert"
              aria-label="Close"
            ></button>
          </div>
        </div>
        <% } %> <% if(error && error.length){ %>
        <div class="container mt-3">
          <div
            class="alert alert-danger alert-dismissible fade show"
            role="alert"
          >
            <%= error[0] %>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="alert"
              aria-label="Close"
            ></button>
          </div>
        </div>
        <% } %>
        <h3 class="p-1"><%= selectedListing.title %></h3>

        <div
          class="card mx-auto mb-3"
          style="width: 100%; max-width: 33rem; border: none"
        >
          <img
            src="<%= selectedListing.image.url %>"
            class="card-img-top-show show-img-detail"
            alt="<%= selectedListing.filename %>"
            style="object-fit: cover; margin-bottom: 0"
          />
        </div>
        
     


        <!-- ✅ Change #1: ek wrapper div banaya same width jaisa image -->
        <div class="p-1" style="max-width: 30rem">
          <ul
            class="list-unstyled"
            style="text-align: left; font-size: 1.1rem; margin-top: -10px"
          >
            <li>
              <b>Owner: @</b>
              <i><%= selectedListing.owner.username %></i>
            </li>
            <br />
            <li>
              <b>Description:</b>
              <%= selectedListing.description %>
            </li>
            <li>
              <b>Price:</b> &#8377; <%=
              selectedListing.price.toLocaleString("en-IN") %>
            </li>
            <li>
              <b>Location:</b>
              <%= selectedListing.location %>
            </li>
            <li>
              <b>Country:</b>
              <%= selectedListing.country %>
            </li>
          </ul>
        </div>

        <div class="d-flex justify-content-start gap-2">
          <% if(currUser && selectedListing.owner._id.equals(currUser._id)) { %>
          <form action="/listings/<%= selectedListing._id %>/edit" method="get">
            <button class="btn btn-warning">Edit</button>
          </form>
          <form
            method="post"
            action="/listings/<%= selectedListing._id %>?_method=delete"
          >
            <button class="btn btn-danger">Delete</button>
          </form>
          <% } %>
        </div>

        <hr />

        <div class="col-12" style="max-width: 30rem">
          <% if(currUser) { %>
          <h4>Leave your Review</h4>
          <form
            action="/listings/<%= selectedListing._id %>/reviews"
            method="post"
            novalidate
            class="needs-validation"
          >
            <div class="mb-3">
              <label for="rating" class="form-label" style="text-align: left"
                >Rating</label
              >
              <input
                type="range"
                min="1"
                max="5"
                name="review[rating]"
                class="form-control-range"
              />
            </div>
            <div class="mb-3">
              <label for="comment" class="form-label" style="text-align: left"
                ><b>Comment</b></label
              >
              <textarea
                name="review[comment]"
                id="comment"
                class="form-control"
                cols="50"
                rows="5"
                required
              ></textarea>
              <div class="invalid-feedback">Comment must not be empty</div>
            </div>
            <button type="submit" class="btn btn-outline-dark">Submit</button>
          </form>
        </div>
        <% } %>
        <hr />
        <div>
          <h4>All Reviews</h4>
          <div class="row">
            <% for(review of selectedListing.reviews) { %>
            <div class="card col-6 ms-1 mb-3">
              <div class="card-body">
                <p class="card-title"><b><%=   review.author.username %></b></p>
                <p class="card-text m-0"><%= review.comment %></p>
                <p class="card-text m-0"><%= review.rating%> star</p>
              </div>
            </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
    <script src="/js/script.js"></script>
  </body>
</html>
