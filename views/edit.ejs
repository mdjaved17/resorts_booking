<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style.css" />
  </head>

  <body>
    <header class="fixed-top">
      <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
          <!-- Logo + Brand -->
          <a class="navbar-brand d-flex align-items-center" href="/listings">
            <img
              src="/images/logo.png"
              alt="Logo"
              width="45"
              height="45"
              class="me-2 rounded-circle border border-2 border-primary"
            />
            <span class="fw-bold text-primary fs-4">Resorts</span>
          </a>

          <!-- Toggle button for mobile -->
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarContent"
            aria-controls="navbarContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>

          <!-- Links + Search -->
          <div class="collapse navbar-collapse" id="navbarContent">
            <!-- Navigation links -->
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a
                  class="nav-link active fw-semibold"
                  aria-current="page"
                  href="/listings"
                  >Home</a
                >
              </li>
              <li class="nav-item">
                <a class="nav-link fw-semibold" href="#">About</a>
              </li>
              <li class="nav-item">
                <a class="nav-link fw-semibold" href="#">Contact</a>
              </li>
            </ul>

            <!-- Search bar -->
            <form class="d-flex ms-lg-3 mt-2 mt-lg-0">
              <input
                class="form-control me-2 rounded-pill"
                type="search"
                placeholder="Search resorts..."
                aria-label="Search"

              />
              <button class="btn btn-primary rounded-pill px-3" type="submit">
                Search
              </button>
            </form>
          </div>
        </div>
      </nav>
    </header>
    <form
      method="post"
      action="/listings/<%= selectedListing._id%>?_method=put"
      novalidate
      class="needs-validation"
      enctype="multipart/form-data"
    >
      <div class="listing-form">
        <div class="mb-3">
          <label for="title" class="form-label"><b>Title</b></label>
          <input
            type="text"
            class="form-control"
            name="title"
            value="<%=selectedListing.title%>"
            required
          />
          <div class="invalid-feedback">Title must be filled</div>
        </div>
        <div class="mb-3">
          <label for="description" class="form-label"><b>Description</b></label>
          <textarea
            name="description"
            id=""
            class="form-control"
            rows="4"
            cols="20"
            required
          > 
                <%= selectedListing.description%></textarea
          >
          <div class="invalid-feedback">Description should be valid</div>
        </div>

        <!-- upload images -->
        <div class="mb-3">
          <label for="image" class="form-label"
            >Upload images of your resort</label
          >
          <input type="file" class="form-control" name="image" />
          required  
        </div>

        <!-- Hidden field to preserve filename -->
        <input
          type="hidden"
          name="image[filename]"
          value="<%= selectedListing.image.filename %>"
        />

        <div class="row">
          <div class="col-md-6 mb-3">
            <label for="price" class="form-label">Price</label>
            <div class="input-group">
              <span class="input-group-text">&#8377;</span>
              <input
                type="number"
                name="price"
                class="form-control"
                id="price"
                value="<%= selectedListing.price%>"
                required
              />
            </div>
            <div class="invalid-feedback">Price should be valid</div>
          </div>

          <div class="col-md-6 mb-3">
            <label for="country" class="form-label">Country</label>
            <input
              type="text"
              name="country"
              class="form-control"
              id="country"
              value="<%= selectedListing.country%>"
              required
            />
            <div class="invalid-feedback">Country should be valid</div>
          </div>
          <div class="mb-3">
            <label for="location" class="form-label"><b>Location</b></label>
            <input
              type="text"
              name="location"
              class="form-control"
              value="<%= selectedListing.location%>"
              required
            />
            <div class="invalid-feedback">Location should be valid</div>
          </div>
          <div class="mb-3">
            <button type="submit" class="btn btn-primary w-100">Submit</button>
          </div>
        </div>
      </div>
    </form>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
    <script src="/js/script.js"></script>
  </body>
</html>
